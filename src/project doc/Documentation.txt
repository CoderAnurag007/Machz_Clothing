 1. React context = react context is the mechanism in the react to get the data and post the data at a place from where any children component  from anywhere can access it there is no  need of passing data from parent to children the to grandchildren even parent dont need that data we still have to pass through them which is not an effiecient way of doing that So there is the concept of react context came in the place.